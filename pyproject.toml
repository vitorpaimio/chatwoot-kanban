[project]
name = "chatwoot-kanban"
version = "0.1.0"
description = "Integração de Kanban e tarefas com o Chatwoot CRM"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
pythonpath = ["."]
